---
import { getCollection, render } from "astro:content";
import Layout from "../layouts/Layout.astro";
import { CATEGORY_ORDER, categoryOf, labelFor } from "../utils/categories";

function groupByCategory(docs: Awaited<ReturnType<typeof getCollection<"docs">>>) {
  const groups = new Map<string, typeof docs>();

  for (const doc of docs) {
    const category = categoryOf(doc.id);
    const group = groups.get(category) ?? [];
    group.push(doc);
    groups.set(category, group);
  }

  for (const group of groups.values()) {
    group.sort((a, b) => {
      if (a.id.endsWith("/index")) return -1;
      if (b.id.endsWith("/index")) return 1;
      return a.data.title.localeCompare(b.data.title);
    });
  }

  const orderedCategories = [
    ...CATEGORY_ORDER.filter((category) => groups.has(category)),
    ...[...groups.keys()].filter((category) => !CATEGORY_ORDER.includes(category)).sort(),
  ];

  return orderedCategories.map((category) => ({
    category,
    label: labelFor(category),
    docs: groups.get(category)!,
  }));
}

export async function getStaticPaths() {
  const docsEntries = await getCollection("docs");
  return docsEntries.map((entry) => ({
    params: { slug: entry.id },
    props: { entry, allDocs: docsEntries },
  }));
}

const { entry, allDocs } = Astro.props;
const { Content } = await render(entry);

const base = import.meta.env.BASE_URL.replace(/\/$/, "");
const navGroups = groupByCategory(allDocs);
---

<Layout title={entry.data.title} description={entry.data.description}>
  <div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8 py-12">
    <div class="flex flex-col lg:flex-row gap-12">
      <aside class="hidden lg:block w-64 shrink-0">
        <nav class="sticky top-24 max-h-[calc(100vh-7rem)] overflow-y-auto pr-2">
          <h3 class="font-semibold text-zinc-900 dark:text-white mb-4">
            Navigation
          </h3>
          {
            navGroups.map((group) => (
              <div class="mb-6">
                <h4 class="text-xs font-semibold uppercase tracking-wide text-zinc-400 dark:text-zinc-500 mb-2">
                  {group.label}
                </h4>
                <ul class="space-y-3 text-sm text-zinc-600 dark:text-zinc-400">
                  {group.docs.map((doc) => (
                    <li>
                      <a
                        href={`${base}/${doc.id}`}
                        class={`transition-colors ${entry.id === doc.id ? "text-emerald-500 dark:text-emerald-400 font-semibold" : "hover:text-zinc-900 dark:hover:text-white"}`}
                      >
                        {doc.data.title}
                      </a>
                    </li>
                  ))}
                </ul>
              </div>
            ))
          }
        </nav>
      </aside>

      <article
        class="prose dark:prose-invert prose-zinc prose-a:text-emerald-500 dark:prose-a:text-emerald-400 prose-a:hover:text-emerald-600 dark:prose-a:hover:text-emerald-300 max-w-3xl mx-auto lg:mx-0"
      >
        <h1>{entry.data.title}</h1>
        <Content />
      </article>
    </div>
  </div>
</Layout>
